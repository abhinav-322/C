#include <stdio.h>
#include <stdlib.h>
#include <math.h>

int main()
{
     float a,b,c;
     printf("enter the sides of triangles");
     scanf("%f %f %f",&a,&b,&c);

    else if(a==b==c)
        printf("triangle is equilateral");
    else if((pow(a,2.0))+(pow(b,2.0))==pow(c,2.0) || (pow(b,2.0))+(pow(c,2.0))==pow(a,2.0) || (pow(a,2.0))+(pow(c,2.0))==pow(b,2.0));
        printf("triangle is right angle triangle");
    else if(a==b && c==!b || c==b && a==!b || a==c && c==!b )
        printf("triangle is isoceles");
    else if
        (printf("triangle is scalen"));
    return 0;



}
